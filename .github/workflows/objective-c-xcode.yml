name: Build CheatEngine IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15  # hoặc macos-latest nếu muốn tự động cập nhật
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.0'  # sẵn có trên macos-15, hoặc dùng 'latest-stable'

      - name: Build Archive
        run: |
          xcodebuild archive \
            -project CheatEngine.xcodeproj \
            -scheme CheatEngine \
            -archivePath ./build/CheatEngine.xcarchive \
            -destination 'generic/platform=iOS' \
            CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO

      - name: Create IPA
        run: |
          mkdir Payload
          cp -r ./build/CheatEngine.xcarchive/Products/Applications/CheatEngine.app Payload/
          zip -r CheatEngine.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4  # Sửa từ v3 -> v4
        with:
          name: CheatEngine.ipa
          path: CheatEngine.ipa
